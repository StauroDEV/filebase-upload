{
  "fmt": {
    "semiColons": false,
    "singleQuote": true,
    "lineWidth": 120
  },
  "tasks": {
    "npm:prepare": "deno run -A scripts/npm.ts",
    "npm:publish": "cd npm && pnpm publish && git push origin --tags"
  }
}
